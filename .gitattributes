# Binary signatures → stay in LFS, no newline/encoding changes
*.mat  filter=lfs diff=lfs merge=lfs -text

# Project configuration files → always UTF‑8 and LF
*.yml   text working-tree-encoding=UTF-8 eol=lf
*.yaml  text working-tree-encoding=UTF-8 eol=lf

# (Optional) pin other text files to LF as well
*.py    text eol=lf
*.md    text eol=lf